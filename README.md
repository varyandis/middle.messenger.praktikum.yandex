# 💬 Webchat

**Webchat** — учебный проект мессенджера, реализованный как **SPA** без серверного рендеринга.
Интерфейс построен на собственном компонентном подходе с базовым классом `Block` и событийной системой `EventBus`.

Проект написан на **TypeScript**, собирается с помощью **Vite** и развёрнут на **Netlify**.

---

## 🚀 Демо

👉 [https://messengerwebchatnow.netlify.app/](https://messengerwebchatnow.netlify.app/)

---

## 📄 Страницы и маршруты

Приложение использует собственный клиентский роутер с поддержкой History API и навигации назад/вперёд.

| Страница             | Маршрут                      |
| -------------------- | ---------------------------- |
| Вход                 | `/`                          |
| Регистрация          | `/sign-up`                   |
| Чаты                 | `/messenger`                 |
| Профиль пользователя | `/settings`                  |
| Изменить данные      | `/settings/edit`             |
| Изменить пароль      | `/settings/password`         |
| Ошибка 404           | любой несуществующий маршрут |
| Ошибка 500           | `/500`                       |

Поддерживается обновление страницы (F5) на любом маршруте благодаря настройке Netlify redirects.

---

## 🔧 Используемые технологии

### Основное

* **TypeScript**
* **Vite** — сборка и dev-сервер
* **Handlebars** — шаблонизатор
* **PostCSS**

  * `postcss-nested`
  * `postcss-preset-env`
* **EditorConfig**

### Линтинг и качество кода

* **ESLint**
* **Stylelint**
* **TypeScript type-checking** (`tsc --noEmit`)

Все проверки объединены в команду:

```bash
npm run lint
```

---

## 🧠 Архитектура

* **Block** — базовый класс для компонентов и страниц
* **EventBus** — система событий и жизненного цикла:

  * `init`
  * `componentDidMount`
  * `componentDidUpdate`
  * `componentDidHide`
* Компонентный подход без сторонних UI-библиотек
* Разделение логики, шаблонов и стилей по страницам
* Собственный клиентский роутер

---

## 🌐 HTTP и WebSocket

### HTTP API

* Собственный класс **HTTPTransport** на базе `XMLHttpRequest`
* Поддержка:

  * `GET`, `POST`, `PUT`, `DELETE`
  * JSON и `FormData`
  * query-параметров
  * обработки ошибок
* Интеграция с API Яндекс.Практикума:

  * авторизация
  * регистрация
  * работа с профилем
  * чаты и пользователи

### WebSocket

* Реализован **real-time чат**
* Подключение по токену чата
* Получение истории сообщений
* Отправка и получение сообщений в реальном времени
* Корректное закрытие WebSocket при уходе со страницы
* Хранение сообщений в Store (`messagesByChat`)

---

## 🔐 Безопасность

* Защита от **XSS**

  * пользовательский ввод рендерится только через `{{ }}` Handlebars
  * не используется `{{{ }}}`
  * отсутствует вставка пользовательских данных через `innerHTML`
* Простая проверка авторизации в роутере

  * неавторизованный пользователь перенаправляется на `/`

---

## 💻 Команды проекта

| Команда              | Описание                                |
| -------------------- | --------------------------------------- |
| `npm run dev`        | Запуск dev-сервера (Vite)               |
| `npm run build`      | Сборка проекта в `dist`                 |
| `npm run preview`    | Просмотр собранного проекта             |
| `npm run deploy`     | Публикация в ветку `deploy` для Netlify |
| `npm run lint`       | ESLint + Stylelint + TypeScript         |
| `npm run lint:ts`    | Проверка TypeScript                     |
| `npm run lint:css`   | Проверка Stylelint                      |
| `npm run lint:types` | `tsc --noEmit`                          |

---

## 📂 Структура проекта

```bash
src/
├── api/                # API-клиенты (Auth, User, Chats)
├── controllers/        # Контроллеры бизнес-логики
├── core/               # Block, EventBus, Router, Store, HTTPTransport, WSTransport
├── pages/              # Страницы приложения
│   ├── login/
│   ├── registration/
│   ├── chats/
│   ├── profile/
│   ├── editProfile/
│   ├── editPassword/
│   ├── error404/
│   └── error500/
├── styles/             # Общие стили и переменные
├── utils/              # Валидация и вспомогательные функции
├── main.ts             # Точка входа и инициализация приложения
└── config/             # Конфигурации (API, env)
```

---

## 🧩 О проекте

Текущая версия Webchat реализует:

* SPA-архитектуру без серверного рендеринга
* собственный компонентный подход
* авторизацию и регистрацию пользователей
* работу с профилем
* создание и удаление чатов
* добавление и удаление пользователей из чата
* real-time сообщения через WebSocket
* хранение состояния в Store
* клиентский роутинг с защитой маршрутов
* обработку ошибок и страницы 404 / 500
* деплой на Netlify

Проект развивается поэтапно в рамках учебных спринтов.
